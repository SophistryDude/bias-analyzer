services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bias_analyzer
      POSTGRES_PASSWORD: bias_analyzer_dev
      POSTGRES_DB: bias_analyzer
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
